import dataset from "../dataset";
import OneElem from "../components/OneElement";
import { useState } from "react";
import _ from "lodash";

// const arr = ;

const Elements = (props) => {


  
  return (
    <div>
      <OneElem></OneElem>
      <button
        onClick={() => {
        }}
      >
        button
      </button>
    </div>
  );
};
export default Elements;
